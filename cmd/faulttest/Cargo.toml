cargo-features = ["per-package-target"]

[package]
name = "faulttest"
version = "0.1.0"
edition = "2024"
default-target = "aarch64-unknown-none"

[dependencies]
r9x-std = { path = "../../std" }

[lints]
workspace = true
